# SQL

Migrations có phiên bản nằm trong migrations/; truy vấn đối soát nằm trong quality/. Chưa có migration hoặc kết nối PostgreSQL. Các schema staging, raw, processed, audit, results sẽ được triển khai theo Phase 2.
