version: 1
level: INFO
format: "%(asctime)s | %(levelname)s | %(name)s | run=%(run_id)s | snapshot=%(snapshot_id)s | %(message)s"
console: true
file: false
context_fields:
  - run_id
  - snapshot_id
  - pipeline
  - step
redact_keys:
  - password
  - secret
  - token
  - credential
  - api_key
